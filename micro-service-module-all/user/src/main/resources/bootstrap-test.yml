spring:
  #redis
  redis:
    host: 127.0.0.1
    database: 2
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      username: nacos
      password: nacos

seata-server:
  config:
    group: SEATA_GROUP
    namespace: #SEATA_GROUP
  discovery:
    group: SEATA_GROUP
    namespace: #SEATA_GROUP